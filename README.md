# WAV_Reverser
Reverse the audio of a .wav file with stereo audio

# Run Instructions
* compile wav.c, main.c, and file_lib.c
* run program with 2 arguments, the file path to the .wav to reverse, and the file path to write the reversed audio
